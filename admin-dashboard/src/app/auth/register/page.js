import { ModernRegisterView } from 'src/sections/auth/jwt';

// ----------------------------------------------------------------------

export const metadata = {
    title: 'Register',
};

export default function RegisterPage() {
    return <ModernRegisterView />;
}
