# ─── Server ───
PORT=4000
NODE_ENV=development

# ─── Database (same as Python news-engine) ───
DB_HOST=python-db
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=news_engine

# ─── JWT ───
JWT_SECRET=change-me-to-a-long-random-string-in-production
JWT_EXPIRES_IN=3d
